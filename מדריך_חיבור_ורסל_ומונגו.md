# 🚀 מדריך שלב אחר שלב להגדרת MongoDB ב-Vercel

הסיבה לשגיאה היא שהאתר ב-Vercel לא יודע איך להתחבר למסד הנתונים, מפני שחסר לו משתנה הסביבה `MONGODB_URI`. 
בצע את השלבים הבאים בדיוק כדי לסדר את זה בתוך 2 דקות.

---

## 📍 שלב 1: כניסה להגדרות הפרויקט ב-Vercel
1. כנס לקישור הבא: **[לוח הבקרה של Vercel](https://vercel.com/dashboard)**.
2. התחבר לחשבון שלך ולחץ על הפרויקט של המערכת (למשל: `fiesta-dashboard` או `fiesta_sapakim`).
3. בתפריט העליון של הפרויקט, לחץ על הלשונית **Settings** (הגדרות).

---

## 📍 שלב 2: הוספת משתנה הסביבה (Environment Variable)
1. בתפריט הצדדי השמאלי, לחץ על האפשרות **Environment Variables**.
2. תחת הכותרת **Key**, הקלד בדיוק את השם הבא באותיות גדולות (ללא רווחים):
   ```text
   MONGODB_URI
   ```
3. תחת הכותרת **Value**, הדבק את מחרוזת החיבור מ-MongoDB Atlas:
   - היכנס ל-[MongoDB Atlas](https://cloud.mongodb.com) → Connect → Drivers
   - העתק את ה-URI (כולל שם המשתמש והסיסמה)
   - ודא שהנתיב מסתיים ב-`/fiesta_crm`
   - אם בסיסמה יש תווים מיוחדים (`!`, `?`, `@` וכו') — יש לקודד אותם ב-URL encoding
4. ודא שכל תיבות הסימון מסומנות (Production, Preview, Development).
5. לחץ על הכפתור הכחול **Save** או **Add** כדי לשמור את המשתנה!

> **אבטחה:** לעולם אל תשמור סיסמאות בקובץ markdown או ב-git. השתמש רק ב-Environment Variables של Vercel.

---

## 📍 שלב 3: ביצוע Redeploy (בנייה מחדש)
*משתני סביבה בשרת נכנסים לתוקף רק לאחר בנייה מחדש של האתר.*

1. בתפריט העליון של דף הפרויקט ב-Vercel, לחץ על הלשונית **Deployments**.
2. בשורה של הפריסה האחרונה שלך (הכי עליונה ברשימה), לחץ על כפתור **3 הנקודות (...)** בצד ימין.
3. לחץ על האפשרות **Redeploy** (פריסה מחדש).
4. בחלון הקופץ הקטן, לחץ שוב על כפתור **Redeploy** הכחול לאישור.

---

## 🎉 זהו זה!
* כעת יתחיל תהליך בנייה שייקח כדקה אחת (תוכל לראות עיגול מסתובב).
* ברגע שהעיגול יהפוך לירוק ויהיה כתוב **Ready**, כנס לאתר שלכם!
* התחבר עם הסיסמה שהוגדרה במערכת (לא לשתף בקבצי תיעוד).
* סמן ספק כ-"לא מעוניין" או "לא זמין" ותראה שהכל עובד חלק וללא שגיאות!

## הגדרה מקומית
העתק `.env.example` ל-`.env.local` והגדר:
```bash
MONGODB_URI=mongodb+srv://...
```
